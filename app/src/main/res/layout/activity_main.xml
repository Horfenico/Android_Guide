<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:background="@drawable/back">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/terminology_button"
        android:id="@+id/terminology_button"
        android:layout_marginTop="168dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="displayTerminology"
        android:textColor="#ffffffff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/websites_button"
        android:id="@+id/websites_button"
        android:layout_centerVertical="true"
        android:layout_alignRight="@+id/terminology_button"
        android:layout_alignEnd="@+id/terminology_button"
        android:layout_alignLeft="@+id/terminology_button"
        android:layout_alignStart="@+id/terminology_button"
        android:textColor="#ffffffff" />
</RelativeLayout>
